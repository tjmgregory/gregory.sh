name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install

      - name: Type check
        run: bun run check

      - name: Newsroom client is what the pinned schema generates
        run: bun run newsroom:check

      - name: Test
        run: bun run test:run

      - name: Test shared health runtime
        run: bun run test:health-state

      - name: Verify health vendor
        run: bun run check:health-vendor

      - name: Build
        run: bun run build
