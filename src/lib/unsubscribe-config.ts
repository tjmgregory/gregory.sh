/**
 * Audience store name signed into one-click unsubscribe tokens. The sender
 * signs the same name, so changing it invalidates every link already sent.
 */
export const UNSUBSCRIBE_AUDIENCE = 'gregory_subscribers';
